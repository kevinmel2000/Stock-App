<resources>

    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp</dimen>

    <!-- fragment detail text sizes -->
    <dimen name="detail_title_text_size">24sp</dimen>

    <!-- extra_detail text sizes -->
    <dimen name="extra_detail_header_text_size">12sp</dimen>
    <dimen name="extra_detail_text_size">14sp</dimen>

    <!-- Needed if we aren't including the Material Design Library -->
    <dimen name="appbar_elevation">4dp</dimen>

    <!-- The amount we want the details view to overlap the app bar -->
    <dimen name="details_app_bar_overlap">24dp</dimen>

    <!-- Widget -->
    <dimen name="widget_default_height">40dp</dimen>
    <dimen name="widget_default_width">40dp</dimen>

</resources>
